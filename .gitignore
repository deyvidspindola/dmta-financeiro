# Cada app do monorepo tem seu próprio .gitignore (apps/api, apps/web,
# apps/mobile quando existirem) — regras específicas de stack ficam lá,
# não aqui. Isto aqui é só o que vale pro repositório inteiro.

.DS_Store
/.fleet
/.idea
/.nova
/.vscode
/.zed

# --- Nunca versionar ---
deploy_key
deploy_key.pub
*.pem
/.deploy-setup

# --- Credenciais coladas na raiz (nunca commitar) ---
/dados de acesso pluggy.txt
/Criar um Connect Token.txt
