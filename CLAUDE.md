# dmta-financeiro (raiz do monorepo)

Antes de qualquer trabalho, leia `docs/00_LEIA_ME.md` — define a ordem de
leitura do restante da documentação e a fase atual em `PROGRESSO.md`.

Este arquivo é só o ponteiro do monorepo. Regras específicas de código:

- `apps/api/CLAUDE.md` + skill `padroes-laravel-dmta` — convenções Laravel.
- `apps/web` e `apps/mobile` terão seu próprio `CLAUDE.md` quando criados.

Não decida arquitetura, stack ou escopo por conta própria — está tudo
decidido em `docs/` (registros D-xx e DT-xx). Se algo parecer em aberto,
confira `00_REGISTRO_DE_DECISOES.md` e `00_DECISOES_TECNICAS.md` antes de
perguntar ou de assumir.
